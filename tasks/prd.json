{
  "tasks": [
    {
      "id": "1",
      "title": "Add API rate limiting for scrape/OCR",
      "acceptanceCriteria": "Rate limits apply to /api/scrape and /api/ocr with 429 responses; limits configurable; does not block health or read-only endpoints",
      "passes": true
    },
    {
      "id": "2",
      "title": "Add API authentication",
      "acceptanceCriteria": "Requests require API key header for /api/scrape and /api/ocr; unauthenticated requests get 401; key is configurable",
      "passes": true
    }
  ]
}
